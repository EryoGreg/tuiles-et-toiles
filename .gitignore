node_modules/
dist/
release/
*.log

# Base locale et images : regenerees par l'import, inutile de les versionner
data/tuiles.db
data/tuiles.db-wal
data/tuiles.db-shm

# Jetons OAuth : ne doivent jamais partir dans un depot
.credentials/
*.token.json
client_secret*.json

# Client OAuth Google (embarque dans l exe, pas dans le depot)
src/main/oauth-client.json
src/mobile/google-config.json

# Fichiers temporaires SQLite et journaux d'execution
*.db-wal
*.db-shm
data/logs/

# Cache des grandes images telechargees (dev)
data/images-cache/

# Copies de securite automatiques (en dev ; Documents\ une fois empaquete)
data/sauvegardes/

# Identite de l'installation (synchro) : propre a chaque poste
data/appareil.json

# Point de reception des rapports d erreur (URL du script Google + cle) : pas dans un depot public
src/main/rapport-config.json
dist-mobile/
